{
  "name": "qs",
  "version": "0.1.0",
  "description": "Bored of typing \"document.querySelector()\" ? use qs : continue",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf dist && babel src -d dist",
    "test": "jest"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-minify": "^0.2.0",
    "jest": "^22.0.4",
    "rimraf": "^2.6.2"
  }
}
